1 - обратить внимание, селект (Тип мотоцикла) первый options  скрывается с помощью js  и должен всегда находится на первом месте.
так же value должно быть пустое для проверки.
2 - в связаных списках нужно учитывать что 3-й  список орейнтируется по id , там в value должен быть id , так же в объекте должны быть
первыми элементами (название селекта - например Город  , вторым - сброс) это сделано для того, чтобы была кнопка сброса как по дизайну и
по нажатию подкидывался первый елемент, который по умолчанию скрыт. Так же учесть что ключ должен начинатся с цыфры 3 , потому что первый будет всегда
елемент по умолчанию, а второй кнопка сброса.
3 - в подборе модели и марке используются шаблоны которые в js , туда нужно передавать обхекты и все будет выводится астоматически,
для отображенение марки используется 2-ва объекста , так же и для модели. 1-й для популярных, второй для полного списка.
4 - в выборе цвета - цвет распологается в data-color и добавляется фоном к блоку, нужно передавать цвет в атрибут.
5 - проверка производится тех элементов, на которых есть класс (check-block)
чтобы отобразилась одибка нужно добавить (error-form) в первых пяти step-form__inner есть примеры, дальше не делал потому что все аналогично.
чтобы проверить чекбоксы, есть функция которая ищет чтобы был хоть один отмечаный, для этого в form-group нужно указать
два класса (form-checked check-block)

Дороботки

где выбирается бренд обязательно добавить класс row--brand (там где общая обертка form-row)
так же поменять в темплате когда мы добавляем новуб строку, чтобы класс так же добавлялся, это нужно для сохраниеня параметров
В мультиселектах, которые выводятся сразу из верстки, а не добаляются js проставить value равные контенту, как пример(Тип мотоцикла)
добавл новые стили в само конце

сдулал добавление колличиство выбранных паремтров фильтра, они скрываются и добавляются, для этого используется функция cntParam
ее нужно вызывать почти везде где добавляется и снимается класс, в коде я это делаю, если идет перезагрзка страницы и есть выбранные параметры, нужно вставить
колличиство, функция не считает их при рендере
чтобы сбрасывать селекты поменял код в main.js , на строке 21 добавил массив в который прокидываю все селекты, чтобы потом в слушателе
их скидывать, находится на 46 строке
так же ждя сброса в массив закидываю и города в связаном списке

для отображение выбраных селектов в конце есть функция, которая запускается после полной прогрузки дома, класс нужно использовать
store-active , я добавил на двух элементах на странице ради примера. Добавлял только на селекты как и говорила.